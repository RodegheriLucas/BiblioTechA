@model IEnumerable<BiblioTechA.Models.Pedido>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="text-center">
    <h1>Central de Pedidos</h1>
</div>
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                Data de empréstimo
            </th>
            <th>
                Previsão de devolução
            </th>
            <th>
                Leitor
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Empresta)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Devolve)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Leitor.Nome)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-info">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-secondary">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<p>
    <a asp-action="Create" class="btn btn-outline-success">Create New</a>
</p>
